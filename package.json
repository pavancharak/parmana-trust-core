{
  "name": "parmana-trust-core",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "tsx": "^4.19.0",
    "turbo": "^2.0.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
