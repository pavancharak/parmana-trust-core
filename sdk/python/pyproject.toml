[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "parmana-sdk"
version = "0.1.0"
description = "Python SDK for Parmana Trust Core"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
  "requests>=2.31.0"
]
