import {
  describe,
  expect,
  it
} from "vitest";

describe(
  "server package",
  () => {

    it(
      "placeholder",
      () => {

        expect(
          true
        ).toBe(
          true
        );

      }
    );

  }
);
